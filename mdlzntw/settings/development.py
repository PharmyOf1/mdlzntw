from .base import *

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

#Future Phil: run --insecure on manage.py if you want to false above, otherwise hosting service will load static files